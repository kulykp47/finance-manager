# 💰 Учет личных финансов (Personal Finance Tracker)

Это простое настольное приложение для учета личных финансов, разработанное на **Python** с использованием библиотеки **Tkinter** для графического интерфейса пользователя (GUI).

---

### ✨ Возможности

* **Добавление операций:** Возможность регистрировать доходы и расходы с указанием суммы, категории, даты и описания.
* **Удаление операций:** Удаление выбранной транзакции из истории.
* **Учет баланса:** Автоматическое отслеживание текущего финансового баланса.
* **Аналитика по категориям:** Отображение сводной информации о доходах/расходах по каждой категории.
* **Сохранение данных:** Транзакции сохраняются в файл `transactions.csv` и загружаются при запуске.

---

### 📋 Структура проекта

Проект состоит из трех основных файлов, а также файла для сохранения данных:

| Файл | Описание |
| :--- | :--- |
| `main.py` | Точка входа в приложение. Инициализирует главное окно Tkinter. |
| `classes.py` | Содержит классы для моделирования данных и бизнес-логики (`FinanceManager`, `Transaction`, `Category`). |
| `gui.py` | Содержит класс `FinanceApp`, который отвечает за построение графического интерфейса (Tkinter). |
| `transactions.csv` | Файл, в котором хранятся все финансовые операции. Создается автоматически при первом сохранении. |

---

### 🚀 Запуск приложения

#### Требования

Для запуска приложения требуется установленный **Python 3.x**. Все используемые библиотеки (`tkinter`, `csv`, `datetime`, `enum`) являются **стандартными** для Python и не требуют дополнительной установки.

#### Инструкции

1.  Клонируйте репозиторий или скачайте файлы проекта (`main.py`, `classes.py`, `gui.py`).
2.  Откройте терминал или командную строку в директории с проектом.
3.  Запустите приложение следующей командой:

    ```bash
    python main.py
    ```

---

### 📝 Использование

1.  **Добавить операцию:**
    * Введите **Сумму** операции.
    * Выберите **Категорию** из выпадающего списка.
    * Введите **Дату** в формате `ГГГГ-ММ-ДД` (по умолчанию — текущая дата).
    * (Необязательно) Введите **Описание**.
    * Нажмите кнопку **"Добавить операцию"**.

2.  **Удалить операцию:**
    * Выберите строку в таблице **"История операций"**.
    * Нажмите кнопку **"Удалить выбранную"**.

3.  **Просмотр аналитики:**
    * Нажмите кнопку **"Аналитика"**. Откроется новое окно с суммарными показателями по каждой категории (доходы — зеленым, расходы — красным).

4.  **Баланс:**
    * **"Текущий баланс"** отображается в верхней части окна. **Зеленый** цвет означает положительный баланс, **красный** — отрицательный.
